export interface FlightModel {
    airlineName: string;
    flightCode: string;
    origin: string;
    destination: string;
    departureDate: string;
    arrivalDate: string;
    flightPrice: number;
}